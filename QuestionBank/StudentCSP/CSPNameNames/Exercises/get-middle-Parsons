.. parsonsprob:: get-middle-Parsons
   :author: Barbara  Ericson
   :difficulty: 0.0
   :basecourse: StudentCSP
   :chapter: CSPNameNames
   :subchapter: Exercises
   :topics: CSPNameNames/Exercises
   :from_source: F
   :numbered: left
   :adaptive: 
   :pct_on_first: 0.0763888889
   :total_students_attempting: 144
   :num_students_correct: 131.0
   :mean_clicks_to_correct: 6.1755725191

   Put the code blocks in order below to return the middle characters from the
   passed string.  There are four extra code blocks that are not needed in a correct 
   solution. If the string has less than 3 characters then return the passed 
   string. If the string has an odd length then return the middle character. If the 
   string has an even length return the two middle characters. For example, 
   get_middle(‘abc’) returns ‘b’ and get_middle(‘abcd’) returns ‘bc’.
   -----
   def get_middle(str):
   =====
   Def get_middle(str): #paired
   =====
       num_chars = len(str)
   =====
       mid = num_chars // 2
    =====
       mid = num_chars / 2 #paired
   =====
       if num_chars < 3:
   =====
           return str
   =====
       elif num_chars % 2 == 1:
   =====
       elif num_chars % 2 == 1 #paired
   =====
           return str[mid]
   =====
       else:
   =====
           return str[mid-1:mid+1]
   =====
           return str[mid:mid+2] #paired