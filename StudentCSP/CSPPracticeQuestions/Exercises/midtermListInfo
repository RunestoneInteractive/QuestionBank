
   :author: Barbara  Ericson
   :difficulty: 1.0
   :basecourse: StudentCSP
   :topic: CSPPracticeQuestions/Exercises
   :from_source: F
.. parsonsprob:: midtermListInfo
   :adaptive:
   :numbered: left

   Put the code in order to print the following information from the list of numbers in nums: sum, max, min, and if there is at least one number in nums, print the average.
   -----
   def printInfo(nums):
   =====
      total = sum(nums)
      print(total)
   =====
      print(max(nums))
   =====
      print(min(nums))
   =====
      if len(nums) > 0:
   =====
          print(total / len(nums))

config values (conf.py):

- parsons_div_class - custom CSS class of the component's outermost div
    