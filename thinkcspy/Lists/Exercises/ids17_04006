.. activecode:: ids17_04006
    :author:  KUO TZU-CHI
    :difficulty: 1
    :basecourse: thinkcspy
    :topic: Lists/Exercises
    :from_source: F

    6.7(劉邦鋒，由片語學習C程式設計)回文是一個長度至少為1，且向前讀或向後讀皆相同的整數序列。例如(1,3,5,3,1)、(1,2,2,1)、(1)都是回文。如果我們連接兩個回文就得到一個雙回文。例如(1,3,5,3,1,1,2,2,1)、(1,3,5,3,1,1)都是雙回文。現在給定一個長度為n的整數序列，請找出其中最長的雙回文。

    　輸入第一行為序列長度n。輸入第二行為序列元素。輸出為輸入中最長的雙回文。如果最長的雙回文不只一個，則輸出在最後面的。參數規範：2 <= n <= 100。

    輸入：

    7

    20　1　3　5　3　1　1

    輸出：

    1　3　5　3　1　1

    ~~~~
    
    ====